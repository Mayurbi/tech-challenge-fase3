# Copie para .env e ajuste. NUNCA commite o .env com credenciais reais.

# Base de prontuários (MySQL). O schema é criado automaticamente se não existir.
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/hospital_mco

# Para testar sem MySQL (qualquer URL SQLAlchemy funciona):
# DATABASE_URL=sqlite:///data/prontuarios/prontuarios.db
